import React from 'react';
import styles from './styles';
import colors from './colors';
import {STORAGES,GOOGLE_APIS,SIGN,WAB_URL,GENDER} from './constants';

import {
  spacing,
  scales,
  svgHeight,
  fontSize,
  fonts,
  fontSizes,
  svgWidth,
  deviceDimensions
} from './others';

export {
  styles,
  colors,
  fonts,
  fontSizes,
  svgWidth,
  spacing,
  scales,
  svgHeight,
  fontSize,
  STORAGES,
  GOOGLE_APIS,
  SIGN,
  WAB_URL,
  GENDER,
  deviceDimensions
};
